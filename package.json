{
  "name": "image-processing-toolkit",
  "version": "1.0.0",
  "description": "Advanced image processing and analysis suite with filters, color conversions, edge detection, and quality metrics",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "NODE_OPTIONS=--experimental-vm-modules jest",
    "test:watch": "NODE_OPTIONS=--experimental-vm-modules jest --watch",
    "test:coverage": "NODE_OPTIONS=--experimental-vm-modules jest --coverage",
    "lint": "eslint src tests",
    "lint:fix": "eslint src tests --fix",
    "format": "prettier --write \"src/**/*.js\" \"tests/**/*.js\"",
    "benchmark": "node benchmarks/performance.js"
  },
  "keywords": [
    "image",
    "processing",
    "filters",
    "edge-detection",
    "color-conversion",
    "histogram",
    "quality-metrics"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "sharp": "^0.33.1"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.23.0",
    "babel-jest": "^29.7.0",
    "eslint": "^8.51.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.28.1",
    "jest": "^29.7.0",
    "prettier": "^3.0.3"
  }
}
